{% extends 'base.html.twig' %}

{% block title %}Créer un nouveau local{% endblock %}

{% block body %}
    <div class="page-heading-rent-venue">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <h2>Créer un nouveau local</h2>
                </div>
            </div>
        </div>
    </div>

    <div class="events-form" style="padding-top: 50px;">
        <div class="container">
            <!-- Formulaire pour la création de local -->
            {{ form_start(form) }}

            <div class="row mb-3">
                <div class="col-md-6">
                    <div class="form-group">
                        {{ form_label(form.name, 'Nom du local') }}
                        {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
                        {% for error in form.name.vars.errors %}
                            <div class="alert alert-danger">{{ error.message }}</div>
                        {% endfor %}
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="form-group">
                        {{ form_label(form.adr, 'Adresse') }}
                        {{ form_widget(form.adr, {'attr': {'class': 'form-control'}}) }}
                        {% for error in form.adr.vars.errors %}
                            <div class="alert alert-danger">{{ error.message }}</div>
                        {% endfor %}
                    </div>
                </div>
            </div>

            <div class="row mb-3">
                <!-- Prix avant la capacité -->
                <div class="col-md-6">
                    <div class="form-group">
                        {{ form_label(form.price, 'Prix') }}
                        {{ form_widget(form.price, {'attr': {'class': 'form-control'}}) }}
                        {% for error in form.price.vars.errors %}
                            <div class="alert alert-danger">{{ error.message }}</div>
                        {% endfor %}
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="form-group">
                        {{ form_label(form.capacite, 'Capacité') }}
                        {{ form_widget(form.capacite, {'attr': {'class': 'form-control'}}) }}
                        {% for error in form.capacite.vars.errors %}
                            <div class="alert alert-danger">{{ error.message }}</div>
                        {% endfor %}
                    </div>
                </div>
            </div>

            <div class="row mb-3">
                <div class="col-md-6">
                    <div class="form-group">
                        {{ form_label(form.description, 'Description') }}
                        {{ form_widget(form.description, {'attr': {'class': 'form-control'}}) }}
                        {% for error in form.description.vars.errors %}
                            <div class="alert alert-danger">{{ error.message }}</div>
                        {% endfor %}
                    </div>
                </div>
            </div>

            <!-- Bouton pour enregistrer -->
            <div class="row mb-3">
                <div class="col-md-12 text-center">
                    <button type="submit" class="btn btn-secondary">Create Local</button>
                </div>
            </div>

            {{ form_end(form) }}

            <!-- Lien de retour à la liste des locaux -->

        </div>
    </div>
{% endblock %}
